// Convenience entrypoint so running `node server.js` from repo root works.
// It simply starts the local Node forwarding service implemented under node-service/.
require('./node-service/server.js');
